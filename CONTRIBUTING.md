# 開発への参加

## 最初に読む

実行基盤はGAS、Google Sheets、Google Driveだけです。追加サーバーを必要にする設計へ変更しないでください。[構成](docs/spec/01-overview-architecture.md)と[セキュリティモデル](docs/spec/02-security-model.md)を読み、利用者が行う操作と失敗条件を確認します。

## 開発環境

Node.js 22以上を用い、`npm ci`で開発依存を揃えます。`npm run check`、`npm test`、`npm run check:public`を実行します。画面変更では`npx playwright install chromium`後に`npm run test:e2e`も実行してください。詳細は[テストガイド](docs/testing.md)にあります。

`src/`にNode.js専用API、ES moduleのimport/export、ビルド必須の依存を導入しません。GASのV8で対応する`const`、`let`、テンプレート文字列、配列メソッド等を使えます。新規追加・変更する箇所では`const`/`let`を推奨しますが、既存コード全体を機械的に`var`から書き換えるような全面ES6変換は行わないでください。変更する関数の責務を明確にし、構文の置換だけの大規模差分は避けてください。[Google V8対応](https://developers.google.com/apps-script/guides/v8-runtime)

## 修正とレビュー

1. 作業ブランチを作り、既存の未コミット変更を確認します。複数の担当（人・AIエージェント問わず）が並行して作業する場合は、`git worktree add` で担当ごとに独立した作業ツリーを分け、各担当は自分が所有するファイルだけを変更します。他の担当が所有するファイルの不具合に気づいた場合は、自分で直接広げず、担当と最小再現を記録して引き継ぎます。
2. 不具合は本番ソースをロードする回帰テストで再現します。テストへ業務ロジックをコピーしません。GASのサービス境界だけを代替します。
3. 修正、関連文書の更新、必要なテストを行います。認可はブラウザUIの表示条件だけで守らないでください。
4. 重要な機能・設計・UI変更は別エージェントまたは人によるレビューを受け、指摘に対応します。
5. PRに具体的な変更前後の挙動、検証コマンドと結果、実環境で未検証の項目を書きます。

## 守る境界

- 公開GAS関数は`doGet`と必要な`api*`だけ。管理処理・includeなどは末尾`_`を付けます。
- 全RPCは利用者のメール、許可ドメイン、全体停止を確認します。実行所有者を利用者として扱いません。
- sandboxに`allow-same-origin`を加えません。投稿ソースは親DOMへ`textContent`で表示します。
- Sheetsの書き込みでは数式として評価されない文字列を保存します。変更はロック・監査・失敗補償を含めて考えます。
- 権限・停止状態・公開版はSheetsを正本とし、共有キャッシュに認可を委ねません。
- 呼称は「アーティファクト」、UIアイコンはSVG、スレートと青の既存配色を維持します。
- 秘密、実データ、ログ、個人のclasp設定をコミットしません。セキュリティ問題の報告は[SECURITY.md](SECURITY.md)に従います。

## デプロイ

開発の検証先と本番を分けてください。ローカルテスト成功だけを理由に本番へ反映しません。GASへの更新は[導入](docs/setup.md)・[デプロイ](docs/deployment.md)・[受入試験](docs/acceptance.md)を参照し、対象と既存デプロイIDを確認します。
