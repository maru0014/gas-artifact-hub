# セキュリティポリシー（Security Policy）

本ドキュメントは、GAS Artifact Hub のセキュリティアーキテクチャ、保護の範囲、防御の限界、および脆弱性報告の手順を定めます。

---

## 1. 想定する運用環境と信頼境界

本システムは、同一 Google Workspace ドメイン内で組織の管理者が `USER_DEPLOYING`（自分として実行）かつ `DOMAIN`（組織内限定）でデプロイすることを前提として設計されています。

- **信頼される運用主体（Trusted Operators）**:
  - デプロイ担当管理者、スプレッドシートの編集権限保持者、および Google Apps Script プロジェクトの編集者。
  - これらの運用主体は基盤全体の管理者権限を持つため、アプリケーション層の ACL による制限対象外です。
- **保護対象ユーザー（Protected Users）**:
  - 同一 Google Workspace ドメイン内の一般社員アカウント。
  - スプレッドシート（DB）や専用 Google Drive 保存フォルダへの直接アクセス権を持たず、Web アプリの RPC 境界経由でのみアクセスします。

---

## 2. 多層防御アーキテクチャ（保護の範囲）

本システムは、第三者や悪意のある入力から基盤および利用者を守るため、以下の多層防御を講じています：

### ① 厳格なサーバーサイド認証・認可
- **毎リクエストでの本人識別**: すべての RPC 呼び出しにおいて `Session.getActiveUser().getEmail()` を検証し、空文字・無記名のアクセスは即座に拒否します。`getEffectiveUser()` 等による代替や匿名閲覧は行いません。
- **組織ドメイン検査**: 利用者のドメインが `system_config` の `ALLOWED_DOMAIN` と完全一致するかを検証します。
- **Sheets 正本による排他認可**: 認可判定はスプレッドシートの正本データに対して行われ、管理責任者（custodian）または有効な ACL（`editor` / `viewer`）に基づいてアクセスを許可します。
- **緊急停止の即時反映**: `SYSTEM_ENABLED = false` または個別アーティファクトの `status = disabled` は、キャッシュを介さず次回の API 要求時に即座に遮断されます。

### ② ゼロトラスト・サンドボックス隔離（`null` Origin）
- **`srcdoc` 注入による隔離**: 投稿された HTML は親フレームから `iframe.srcdoc = rawHtml` として注入されます。
- **`allow-same-origin` の完全排除**: iframe の `sandbox` 属性には `allow-scripts allow-downloads allow-forms allow-popups allow-modals` のみを指定し、**`allow-same-origin` は絶対に含めません**。
- これにより、ブラウザによってアーティファクトに不透明オリジン（opaque origin、すなわち `null` origin）が割り当てられ、親フレームの DOM、Cookie、Google セッション、および GAS の実行コンテキストへの直接アクセスがブラウザの同一オリジンポリシーによって遮断されます。

### ③ 蓄積型 XSS（Stored XSS）および CSV/Formula Injection 防止
- **ソース表示のプレーンテキスト描画**: 親フレームでアーティファクトの HTML ソースコードを表示する際、`innerHTML` を一切使用せず、必ず `textContent` として安全に描画します。
- **数式インジェクション防止**: タイトルや説明、監査ログをスプレッドシートに保存する際、先頭が `=` や `'` 等で始まる文字列が意図せず数式として評価されないよう、エスケープ（先頭アポストロフィ付与）処理を行って保存します。

### ④ 静的検査スキャナー（補助判定）と同意モーダル（Consent）
- **タグ走査と正規表現によるスキャナー**: アップロード時に HTML を静的解析し、外部 URL（画像、スクリプト、CSS、fetch、XHR、WebSocket 等）や動的実行（`eval`, `Function`）を抽出します。
- **著名 CDN メタデータ付与**: Google Fonts、Tailwind CSS、Lucide Icons、cdnjs などの正規配信パターンを客観的に分類します。
- **初回同意モーダル**: 警告が含まれるバージョンを実行する際、利用者に検出内容を一覧表示し、明示的な同意ボタン（Consent）を押下するまで iframe をロードしません。

---

## 3. 防御の限界（保証しないこと）

ゼロトラスト隔離を行っていますが、ブラウザの仕様および Web 標準の制約上、以下の脅威を完全に排除することはできません。過剰な安全保証を期待しないでください：

| 限界事項 | 詳細とリスク |
| --- | --- |
| **外部通信とデータ送信** | サンドボックス内であっても、`<form>` 送信、`<img>` タグ、動的 `<script>`、および CORS 許可された `fetch` などによる外部ネットワーク通信は技術的にブロックできません。利用者がアーティファクト内に入力したデータが外部へ送信されるリスクがあります。 |
| **静的スキャナーの網羅性** | 静的解析は補助機能であり、「警告 0 件」は安全の証明ではありません。難読化されたスクリプト、動的文字列結合、未登録の外部通信をすべて検知することは不可能です。警告が 45,000 文字を超えて切り詰められた場合（`SCAN_TRUNCATED`）、ソースコード全文の手動確認が必要です。 |
| **フィッシング・偽画面** | iframe 内で Google や社内システムの偽ログイン画面を表示し、パスワードや機密情報を入力させるフィッシング攻撃をサーバー側で自動判定することは困難です。 |
| **DoS・リソース枯渇** | 無限ループや大量のメモリ確保を行うスクリプトにより、閲覧者のブラウザタブがフリーズ・クラッシュする可能性があります。 |
| **実行済み HTML の強制終了** | すでに利用者のブラウザ上でロードされ実行中の HTML や、ローカルにダウンロードされたファイルをサーバー側から遠隔停止・強制消去することはできません。 |
| **管理者の監査ログ改ざん** | スプレッドシートの直接編集権限を持つ管理者によるシート手動書き換えを防ぐことは Google Sheets の仕様上できません。 |

> **注意**:
> HTML コード内に認証トークン、パスワード、社外秘データを埋め込まないでください。
> 閲覧権限を持つすべてのユーザーが「ソース表示」からコード全文を確認・保存できます。

---

## 4. 脆弱性の報告窓口（Vulnerability Reporting）

本リポジトリまたは導入システムにセキュリティ上の脆弱性を発見した場合は、以下の手順で報告してください。

### GitHub での非公開報告（推奨）
- 本リポジトリが GitHub 上で公開され、メンテナーにより **Private vulnerability reporting** が有効化されている場合は、リポジトリの **Security → Report a vulnerability** から非公開で脆弱性をご報告ください。
- ※現在は公開準備段階です。Private vulnerability reporting は公開時に有効化される設定項目であり、未公開の段階では利用できない場合があります。

### 代替報告手順
- Private vulnerability reporting が利用できない場合、**公開 Issue や Pull Request に脆弱性の詳細、PoC、攻撃コード、個人情報、認証情報を直接書き込まないでください**。
- 公開 Issue には「セキュリティに関する非公開報告を希望します」とだけ記載し、メンテナーからの安全な連絡経路（PGP 暗号化メール等）の案内をお待ちください。

### 報告に含めていただきたい内容
- 影響を受けるコンポーネントまたはファイル（例: `Store.gs`, `Shell.html`）
- 対象コミット SHA またはリリースバージョン
- 脆弱性の種類と影響範囲（例: ACL 迂回、Stored XSS、サンドボックス逸脱）
- 機密情報を除いた最小限の再現手順（PoC）

報告を受領後、開発チームにて内容を精査し、修正対応およびリリースノートでの告知を計画いたします。
